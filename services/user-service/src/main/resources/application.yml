server:
  port: 8001

spring:
  application:
    name: user-service

  jwt:
    secret:
      key: this-is-very-important-secret-key-dama-lala



  datasource:
    url: jdbc:postgresql://localhost:5432/school-system
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

